<% field_title = nil if not defined?(field_title) -%>
<label for="<%= field %>">
 	<%= field_title || field.humanize.downcase %>
</label> 
<%= form.password_field field, 
                        :size => User::PASSWORD_SIZE, 
                        :maxlength => User::PASSWORD_MAX_LENGTH,
						:class => "password" %>